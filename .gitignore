__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.DS_Store
.idea/
.vscode/

# latelock runtime artefacts -- research outputs live in latelock/reports/
latelock/data/
*.sqlite
*.sqlite-*
*.jsonl
HALT
runner.lock

# LLM response cache is committed on purpose -- it makes study 06 re-scorable
# without an API key. Nothing else in reports/ is ignored.
